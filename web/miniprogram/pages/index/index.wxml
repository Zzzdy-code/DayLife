<view class="min-h-screen bg-gray-50 pb-10">
  <!-- 日期显示 -->
  <view class="px-8 py-10 bg-white border-b border-gray-200">
    <text class="text-5xl font-bold text-gray-800">{{currentDate}}</text>
  </view>

  <!-- 日程模块 -->
  <view class="mx-8 mt-6 p-8 bg-white rounded-4">
    <view class="flex items-center mb-6">
      <text class="text-4xl font-bold text-gray-800">📋 今日日程</text>
      <text class="text-3xl text-gray-400 ml-2">({{schedules.length}})</text>
    </view>

    <!-- 日程列表 -->
    <view wx:if="{{schedules.length > 0}}" class="mb-6">
      <schedule-card
        wx:for="{{schedules}}"
        wx:key="id"
        schedule="{{item}}"
        bindtap="handleScheduleTap"
      />
    </view>

    <!-- 空状态 -->
    <view wx:else class="py-15 text-center text-gray-400 text-3xl">
      <text>今天还没有日程</text>
    </view>

    <!-- 添加按钮 -->
    <button 
      class="w-full h-20 leading-20 bg-blue-600 text-white text-3xl rounded-2 mt-6"
      bindtap="handleAddSchedule"
    >
      + 添加日程
    </button>
  </view>
</view>
