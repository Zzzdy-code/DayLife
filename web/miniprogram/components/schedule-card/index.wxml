<view class="flex p-6 mb-6 bg-white rounded-4 shadow-sm" bindtap="handleTap">
  <view class="flex flex-col items-center justify-center min-w-30 mr-6">
    <text class="text-4xl font-bold text-blue-600">{{schedule.startTime || '全天'}}</text>
    <text wx:if="{{schedule.endTime}}" class="text-3xl text-gray-400 mt-2">- {{schedule.endTime}}</text>
  </view>
  
  <view class="flex-1">
    <view class="text-4xl text-gray-800 mb-4">{{schedule.title}}</view>
    <view class="flex items-center flex-wrap">
      <text wx:if="{{schedule.location}}" class="text-3xl text-gray-600 mr-4">📍 {{schedule.location}}</text>
      <text 
        wx:for="{{schedule.tags}}" 
        wx:key="index" 
        class="inline-block px-3 py-1 mr-2 text-2xl text-blue-600 bg-blue-50 rounded"
      >
        {{item}}
      </text>
    </view>
  </view>
</view>
